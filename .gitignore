.DS_Store
*.bak
*.tmp
data/*.json.tmp
__pycache__/
*.pyc

# Superseded by index.html (the live-Overpass prototype). Left on disk so you
# still have it; delete when you're happy with the new site.
alpr-map.html

# harvest checkpoint
data/.harvest-state.json
data/.harvest-state.tmp

# UCDP release download cache (see scripts/harvest_conflicts.py)
data/.ucdp-cache/

# Local dev server config for the Claude Code browser pane
.claude/launch.json
